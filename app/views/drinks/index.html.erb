<br><br><h2>Today's Drinks</h2>

        <% @drinks.reverse.each do |drink| %>
            <span>
                <strong><%= link_to "#{drink.name}", drink_path(drink) %></strong>
                by <em><%= link_to "#{drink.user.name}", user_path(drink.user) %></em><br>
            </span>
        <% end %>
<br><br>
<%= link_to "add drink", new_drink_path %>
